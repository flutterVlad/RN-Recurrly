import "@/global.css";
import React from "react";
import { Text, View } from "react-native";

const Subscriptions = () => {
  return (
    <View>
      <Text className="safe-t">Subscriptions</Text>
    </View>
  );
};

export default Subscriptions;
